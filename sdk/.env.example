# ADLS Gen2 endpoint (DFS endpoint, *.dfs.core.windows.net)
ADLS_DFS_URI=https://mskrblobv2.dfs.core.windows.net/

# 컨테이너(filesystem) 이름
ADLS_CONTAINER=watcher

# (선택) 컨테이너 내부 prefix - 비우면 root 에 yyyy/MM/dd 로 업로드
ADLS_TARGET_PREFIX=
